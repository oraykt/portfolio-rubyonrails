<%= form_with model: @comment, html: { id: "new_comment" }, url: '#' do |f| %>
  <div class="form-group">
    <%= f.label :content, "Make a comment" %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>
  <%= f.submit 'Post Comment', class: 'btn btn-primary' %>
<% end %>