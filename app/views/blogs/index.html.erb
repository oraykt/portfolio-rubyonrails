<div class="col-sm-8 blog-main">
  <div class="d-flex justify-content-center">
    <%= paginate @blogs %>
  </div>
  <div class="container">
    <%= render @blogs %>
  </div>
  <div class="d-flex justify-content-center">
    <%= paginate @blogs %>
  </div>

</div>
