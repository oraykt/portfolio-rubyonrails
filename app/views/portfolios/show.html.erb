<%= link_to "Back", request.referer.present? ? request.referer : root_path %>

<h1>Show</h1>
<div>
  <ul>
      <li>
        <%= link_to "Edit the Portfolio", edit_portfolio_path(@portfolio.id) %>

        <p><%= @portfolio.title %></p>
        <p><%= @portfolio.subtitle %></p>
        <p><%= @portfolio.body %></p>
        <%= image_tag @portfolio.main_image unless @portfolio.main_image.nil? %>

        <h2>Technologies Used:</h2>
        <% @portfolio.technologies.each do |tech| %>
        <p><%= tech.name %></p>
        <% end %>
      </li>
  </ul>
</div>